{
  "name": "imperium-twilight-of-empires",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "IMPERIUM: Twilight of Empires — a browser strategy game set in the late Roman/Byzantine world (~1400–1453).",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "build": "npm run build --workspace @imperium/shared && npm run build --workspace @imperium/server && npm run build --workspace @imperium/client",
    "predev": "npm run build --workspace @imperium/shared",
    "dev": "concurrently -n server,client -c blue,magenta \"npm run dev --workspace @imperium/server\" \"npm run dev --workspace @imperium/client\"",
    "test": "npm run test --workspace @imperium/server",
    "typecheck": "npm run build --workspace @imperium/shared && npm run typecheck --workspace @imperium/server && npm run typecheck --workspace @imperium/client"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.6.3"
  }
}
